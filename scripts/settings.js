function toggle(switchElement) {
  switchElement.classList.toggle("active");
}

function addHeader(title) {}

function addSection(title) {}

function addSwitch(name, defaultState) {}

function addLink(name, destination) {} 